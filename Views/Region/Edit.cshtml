@model VAK.Models.Region

@using (Html.BeginForm()) {
  @Html.HiddenFor(m => m.RegionId)

  @Html.Label("Region Description")
  @Html.TextBoxFor(m => m.RegionDescription)

  <input type="submit" value="Update" />
}