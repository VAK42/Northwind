@model VAK.Models.Shippers

@using (Html.BeginForm()) {
  @Html.HiddenFor(m => m.ShipperId)

  @Html.Label("Company Name")
  @Html.TextBoxFor(m => m.CompanyName)

  @Html.Label("Phone")
  @Html.TextBoxFor(m => m.Phone)

  <input type="submit" value="Update" />
}